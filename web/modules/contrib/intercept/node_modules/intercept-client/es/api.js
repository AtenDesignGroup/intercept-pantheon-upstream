import mapValues from 'lodash/mapValues';
import { ApiManager } from './util/ApiManager';
import models from './models';

var api = mapValues(models, function (model) {
  return new ApiManager({
    model: model,
    priority: 2
  });
});

export default api;