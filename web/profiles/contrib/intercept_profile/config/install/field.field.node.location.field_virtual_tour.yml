langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_virtual_tour
    - node.type.location
  module:
    - text
id: node.location.field_virtual_tour
field_name: field_virtual_tour
entity_type: node
bundle: location
label: 'Virtual Tour'
description: 'Paste the html (including the iframe tags) from Google 360 here. Make sure that the width is set to 100% and not a pixel value. Make sure you change from "Basic HTML" to "Full HTML" and make sure you paste into SOURCE.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: text_long
